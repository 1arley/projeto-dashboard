# ── Production ──────────────────────────────────
# O proxy /api/ e feito pelo nginx, nao pelo Vite.
# O api.js ja prefixa as rotas com v1/, entao a base deve ser apenas /api/.
VITE_API_BASE_URL=/api/
